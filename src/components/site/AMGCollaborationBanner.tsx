import { ContextualPartnershipBanner } from "./ContextualPartnershipBanner";

/**
 * Re-exporting ContextualPartnershipBanner as AMGCollaborationBanner
 * to maintain backward compatibility while ensuring a single, 
 * non-duplicate contextual partnership banner system.
 */
export const AMGCollaborationBanner = ContextualPartnershipBanner;
